{
  "version": 2,
  "buildCommand": "npm run server:build",
  "outputDirectory": ".",
  "functions": {
    "api/server.mjs": {
      "maxDuration": 60,
      "includeFiles": "server_dist/**"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/server" }
  ]
}
